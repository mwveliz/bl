{% extends 'base.html.twig' %}

{% block body %}
    <h1>Rental list</h1>

    <table>
        <thead>
            <tr>
                <th>Description</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for rental in rentals %}
            <tr>
                <td><a href="{{ path('rental_show', { 'id': rental.id }) }}">{{ rental.description }}</a></td>
                <td>{{ rental.id }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('rental_show', { 'id': rental.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('rental_edit', { 'id': rental.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('rental_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
