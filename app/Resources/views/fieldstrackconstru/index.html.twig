{% extends 'base.html.twig' %}

{% block body %}
    <h1>FieldsTrackConstru list</h1>

    <table>
        <thead>
            <tr>
                <th>Description</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fieldsTrackConstru in fieldsTrackConstrus %}
            <tr>
                <td><a href="{{ path('fieldstrackconstru_show', { 'id': fieldsTrackConstru.id }) }}">{{ fieldsTrackConstru.description }}</a></td>
                <td>{{ fieldsTrackConstru.id }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('fieldstrackconstru_show', { 'id': fieldsTrackConstru.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('fieldstrackconstru_edit', { 'id': fieldsTrackConstru.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('fieldstrackconstru_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
