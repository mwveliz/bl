{% extends 'base.html.twig' %}

{% block body %}
    <h1>FieldsTrackConstru</h1>

    <table>
        <tbody>
            <tr>
                <th>Description</th>
                <td>{{ fieldsTrackConstru.description }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ fieldsTrackConstru.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('fieldstrackconstru_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('fieldstrackconstru_edit', { 'id': fieldsTrackConstru.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
