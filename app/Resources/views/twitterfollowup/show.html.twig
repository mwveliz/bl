{% extends 'base.html.twig' %}

{% block body %}
    <h1>Twitterfollowup</h1>

    <table>
        <tbody>
            <tr>
                <th>Account</th>
                <td>{{ twitterfollowup.account }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ twitterfollowup.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('twitterfollowup_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('twitterfollowup_edit', { 'id': twitterfollowup.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
