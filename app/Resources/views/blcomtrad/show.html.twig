{% extends 'base.html.twig' %}

{% block body %}
    <h1>BlComtrad</h1>

    <table>
        <tbody>
            <tr>
                <th>Value</th>
                <td>{{ blComtrad.value }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ blComtrad.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('blcomtrad_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('blcomtrad_edit', { 'id': blComtrad.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
