{% extends 'base.html.twig' %}

{% block body %}
    <h1>FieldsTrackComtrad</h1>

    <table>
        <tbody>
            <tr>
                <th>Description</th>
                <td>{{ fieldsTrackComtrad.description }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ fieldsTrackComtrad.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('fieldstrackcomtrad_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('fieldstrackcomtrad_edit', { 'id': fieldsTrackComtrad.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
