{% extends 'base.html.twig' %}

{% block body %}
    <h1>FieldsTrackRental</h1>

    <table>
        <tbody>
            <tr>
                <th>Description</th>
                <td>{{ fieldsTrackRental.description }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ fieldsTrackRental.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('fieldstrackrental_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('fieldstrackrental_edit', { 'id': fieldsTrackRental.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
