{% extends 'base.html.twig' %}

{% block body %}
    <h1>DashboardPosition list</h1>

    <table>
        <thead>
            <tr>
                <th>Description</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for dashboardPosition in dashboardPositions %}
            <tr>
                <td><a href="{{ path('dashboardposition_show', { 'id': dashboardPosition.id }) }}">{{ dashboardPosition.description }}</a></td>
                <td>{{ dashboardPosition.id }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('dashboardposition_show', { 'id': dashboardPosition.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('dashboardposition_edit', { 'id': dashboardPosition.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('dashboardposition_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
