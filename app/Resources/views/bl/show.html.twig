{% extends 'base.html.twig' %}

{% block body %}
    <h1>Bl</h1>

    <table>
        <tbody>
            <tr>
                <th>Type</th>
                <td>{{ bl.type }}</td>
            </tr>
            <tr>
                <th>Codebl</th>
                <td>{{ bl.codeBl }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ bl.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('bl_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('bl_edit', { 'id': bl.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
