{% extends 'base.html.twig' %}

{% block body %}
    <h1>BlConstru</h1>

    <table>
        <tbody>
            <tr>
                <th>Value</th>
                <td>{{ blConstru.value }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ blConstru.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('blconstru_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('blconstru_edit', { 'id': blConstru.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
