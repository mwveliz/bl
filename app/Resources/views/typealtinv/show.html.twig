{% extends 'base.html.twig' %}

{% block body %}
    <h1>TypeAltinv</h1>

    <table>
        <tbody>
            <tr>
                <th>Description</th>
                <td>{{ typeAltinv.description }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ typeAltinv.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('typealtinv_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('typealtinv_edit', { 'id': typeAltinv.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
