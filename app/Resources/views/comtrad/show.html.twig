{% extends 'base.html.twig' %}

{% block body %}
    <h1>Comtrad</h1>

    <table>
        <tbody>
            <tr>
                <th>Description</th>
                <td>{{ comtrad.description }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ comtrad.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('comtrad_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('comtrad_edit', { 'id': comtrad.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
