{% extends 'base.html.twig' %}

{% block body %}
    <h1>BlRental list</h1>

    <table>
        <thead>
            <tr>
                <th>Value</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for blRental in blRentals %}
            <tr>
                <td><a href="{{ path('blrental_show', { 'id': blRental.id }) }}">{{ blRental.value }}</a></td>
                <td>{{ blRental.id }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('blrental_show', { 'id': blRental.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('blrental_edit', { 'id': blRental.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('blrental_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
