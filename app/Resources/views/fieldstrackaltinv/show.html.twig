{% extends 'base.html.twig' %}

{% block body %}
    <h1>FieldsTrackAltinv</h1>

    <table>
        <tbody>
            <tr>
                <th>Description</th>
                <td>{{ fieldsTrackAltinv.description }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ fieldsTrackAltinv.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('fieldstrackaltinv_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('fieldstrackaltinv_edit', { 'id': fieldsTrackAltinv.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
