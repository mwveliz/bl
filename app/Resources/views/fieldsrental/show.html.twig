{% extends 'base.html.twig' %}

{% block body %}
    <h1>FieldsRental</h1>

    <table>
        <tbody>
            <tr>
                <th>Description</th>
                <td>{{ fieldsRental.description }}</td>
            </tr>
            <tr>
                <th>Widget</th>
                <td>{{ fieldsRental.widget }}</td>
            </tr>
            <tr>
                <th>Trackable</th>
                <td>{% if fieldsRental.trackable %}Yes{% else %}No{% endif %}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ fieldsRental.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('fieldsrental_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('fieldsrental_edit', { 'id': fieldsRental.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
