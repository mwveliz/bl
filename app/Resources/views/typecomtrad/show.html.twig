{% extends 'base.html.twig' %}

{% block body %}
    <h1>TypeComtrad</h1>

    <table>
        <tbody>
            <tr>
                <th>Description</th>
                <td>{{ typeComtrad.description }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ typeComtrad.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('typecomtrad_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('typecomtrad_edit', { 'id': typeComtrad.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
