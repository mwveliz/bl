{% extends 'base.html.twig' %}

{% block body %}
    <h1>TodoPriority list</h1>

    <table>
        <thead>
            <tr>
                <th>Description</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for todoPriority in todoPriorities %}
            <tr>
                <td><a href="{{ path('todopriority_show', { 'id': todoPriority.id }) }}">{{ todoPriority.description }}</a></td>
                <td>{{ todoPriority.id }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('todopriority_show', { 'id': todoPriority.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('todopriority_edit', { 'id': todoPriority.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('todopriority_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
