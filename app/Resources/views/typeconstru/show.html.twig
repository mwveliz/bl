{% extends 'base.html.twig' %}

{% block body %}
    <h1>Type of Construction</h1>

    <table>
        <tbody>
            <tr>
                <th>Description</th>
                <td>{{ typeConstru.description }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ typeConstru.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('typeconstru_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('typeconstru_edit', { 'id': typeConstru.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
