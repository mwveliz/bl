{% extends 'base.html.twig' %}

{% block body %}
    <h1>BlAtlinv</h1>

    <table>
        <tbody>
            <tr>
                <th>Value</th>
                <td>{{ blAtlinv.value }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ blAtlinv.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('blatlinv_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('blatlinv_edit', { 'id': blAtlinv.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
