{% extends 'base.html.twig' %}

{% block body %}
    <h1>TrackComtrad</h1>

    <table>
        <tbody>
            <tr>
                <th>Value</th>
                <td>{{ trackComtrad.value }}</td>
            </tr>
            <tr>
                <th>Datetime</th>
                <td>{% if trackComtrad.datetime %}{{ trackComtrad.datetime|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ trackComtrad.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('trackcomtrad_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('trackcomtrad_edit', { 'id': trackComtrad.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
