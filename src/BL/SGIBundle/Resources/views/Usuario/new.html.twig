{% extends '::base.html.twig' %}

{% block body -%}
    <h3 id="containers">Registrar Usuario</h3>
    <hr/>    
    <div class="col-lg-12">
        <div id='boton'>
            <p class="text-right">
                <a href = "{{ path('usuario') }}" class = "btn btn-info btn-sm">
                    <span class = "glyphicon glyphicon-th-list">                
                    </span> Listado     
                </a>    
            </p>
        </div>
        {{ form(form) }} 
        <br>
        <p class="text-left">
            <a href = "{{ path('usuario') }}" class = "btn btn-info btn-sm">
                <span class = "glyphicon glyphicon-th-list">                
                </span> Listado     
            </a>    
        </p>        
    </div>        
{% endblock %}

