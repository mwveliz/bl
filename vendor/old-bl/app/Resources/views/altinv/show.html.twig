{% extends 'base.html.twig' %}

{% block body %}
    <h1>Altinv</h1>

    <table>
        <tbody>
            <tr>
                <th>Description</th>
                <td>{{ altinv.description }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ altinv.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('altinv_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('altinv_edit', { 'id': altinv.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
