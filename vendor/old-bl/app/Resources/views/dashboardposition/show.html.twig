{% extends 'base.html.twig' %}

{% block body %}
    <h1>DashboardPosition</h1>

    <table>
        <tbody>
            <tr>
                <th>Description</th>
                <td>{{ dashboardPosition.description }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ dashboardPosition.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('dashboardposition_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('dashboardposition_edit', { 'id': dashboardPosition.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
