{% extends 'base.html.twig' %}

{% block body %}
    <h1>BillingDetail</h1>

    <table>
        <tbody>
            <tr>
                <th>Description</th>
                <td>{{ billingDetail.description }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ billingDetail.value }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ billingDetail.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('billingdetail_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('billingdetail_edit', { 'id': billingDetail.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
