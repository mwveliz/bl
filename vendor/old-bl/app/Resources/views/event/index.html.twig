{% extends 'base.html.twig' %}

{% block body %}
    <h1>Event list</h1>

    <table>
        <thead>
            <tr>
                <th>Description</th>
                <th>Datetimestart</th>
                <th>Datetimeend</th>
                <th>Place</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for event in events %}
            <tr>
                <td><a href="{{ path('event_show', { 'id': event.id }) }}">{{ event.description }}</a></td>
                <td>{% if event.datetimeStart %}{{ event.datetimeStart|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if event.datetimeEnd %}{{ event.datetimeEnd|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ event.place }}</td>
                <td>{{ event.id }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('event_show', { 'id': event.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('event_edit', { 'id': event.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('event_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
