{% extends 'base.html.twig' %}

{% block body %}
    <h1>TodoPriority</h1>

    <table>
        <tbody>
            <tr>
                <th>Description</th>
                <td>{{ todoPriority.description }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ todoPriority.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('todopriority_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('todopriority_edit', { 'id': todoPriority.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
